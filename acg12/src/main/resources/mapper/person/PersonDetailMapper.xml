<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.acg12.dao.person.PersonDetailDao">

    <resultMap id="baseResultMap" type="com.acg12.entity.po.person.PersonDetailEntity">
        <result column="personDetailId" property="personDetailId" jdbcType="INTEGER"/>
        <result column="personId" property="personId" jdbcType="INTEGER"/>
        <result column="job" property="job" jdbcType="VARCHAR"/>
        <result column="alias" property="alias" jdbcType="INTEGER"/>
        <result column="otherTitle" property="otherTitle" jdbcType="VARCHAR"/>
        <result column="otherValue" property="otherValue" jdbcType="VARCHAR"/>
    </resultMap>

    <insert id="insert" parameterType="com.acg12.entity.po.person.PersonDetailEntity" useGeneratedKeys="true"
            keyProperty="personDetailId">
        insert into acg_person_detail (personDetailId , personId , job, alias, otherTitle, otherValue)
        values (#{personDetailId,jdbcType=INTEGER},#{personId,jdbcType=INTEGER}, #{job,jdbcType=VARCHAR}, #{alias,jdbcType=VARCHAR}
        , #{otherTitle,jdbcType=VARCHAR}, #{otherValue,jdbcType=VARCHAR})
    </insert>

    <insert id="insertList" parameterType="java.util.List" >
        insert into acg_person_detail (personDetailId , personId , job, alias, otherTitle, otherValue)
        values
        <foreach collection="list" item="item" index="index" separator=",">
            (#{item.personDetailId,jdbcType=INTEGER}, #{item.personId,jdbcType=INTEGER},
            #{item.job,jdbcType=VARCHAR},#{item.alias,jdbcType=VARCHAR},#{item.otherTitle,jdbcType=VARCHAR},#{item.otherValue,jdbcType=VARCHAR}
            )
        </foreach>
        <selectKey resultType="java.lang.Integer" order="AFTER" keyProperty="subjectDetailId">
            SELECT LAST_INSERT_ID();
        </selectKey>
    </insert>

    <update id="update" parameterType="com.acg12.entity.po.subject.SubjectEntity">
        update acg_person_detail set platform=#{platform},title=#{title},cover=#{cover},tags=#{tags},intro=#{intro},voiceActor=#{voiceActor},nowStatus=#{nowStatus}
        ,totalCount=#{totalCount},serializeWeek=#{serializeWeek},serializeTime=#{serializeTime},startPlayTime=#{startPlayTime},updateTime=#{updateTime}
        where bangumitId=#{bangumitId}
    </update>

    <select id="queryList" resultType="com.acg12.entity.po.person.PersonDetailEntity">
        select * from acg_person_detail
    </select>

    <select id="queryByPersonId" parameterType="int" resultType="com.acg12.entity.po.person.PersonDetailEntity">
        select * from acg_person_detail where personId = #{personId}
    </select>

</mapper>